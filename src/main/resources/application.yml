server:
  compression:
    enabled: true
    mime-types: application/json,application/xml,text/html,text/xml,text/plain,application/javascript,text/css

spring:
  data:
    mongodb:
      host: ${EXPERT_DATABASE_URL:localhost}
      database: expert-consultation
